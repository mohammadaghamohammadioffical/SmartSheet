# SmartSheet — مستندات فنی و مسابقه‌ای پروژه

**نوع پروژه:** سامانه خودکار تصحیح OMR (Optical Mark Recognition)  
**فناوری هسته:** بینایی ماشین + OpenCV + منطق تصمیم‌گیری قطعی  
**کاربرد اصلی:** ارزیابی آزمون‌های چندگزینه‌ای  
**هم‌راستایی:** هدف توسعه پایدار ۴ (SDG 4) — آموزش باکیفیت

> **نکته مهم برای ارسال مسابقه:** هسته فعلی SmartSheet یک سامانه پردازش تصویر و بینایی ماشین است و مدل Deep Learning آموزش‌دیده ندارد. بنابراین در فرم مسابقه نباید قابلیت CNN یا یادگیری عمیق فعلی را ادعا کنیم. می‌توان پروژه را به‌عنوان یک راهکار AI-related مبتنی بر Computer Vision معرفی کرد و مسیر توسعه به سمت مدل‌های یادگیری را نیز توضیح داد.

---

## ۱. خلاصه اجرایی

SmartSheet یک سامانه نرم‌افزاری OMR است که عکس یا اسکن برگه چندگزینه‌ای را به نتایج ساختاریافته تصحیح تبدیل می‌کند. سیستم ابتدا کیفیت تصویر را بررسی می‌کند، در صورت امکان محدوده برگه را پیدا و پرسپکتیو آن را اصلاح می‌کند، حباب‌های پاسخ را تشخیص می‌دهد، شبکه هندسی سؤال‌ها را بازسازی می‌کند، هر گزینه را با چند ویژگی تصویری امتیازدهی می‌کند و در نهایت پاسخ تشخیص‌داده‌شده را با کلید پاسخ مقایسه می‌کند.

یکی از اصول مهم طراحی SmartSheet **اتوماسیون محافظه‌کارانه** است: اگر شواهد تصویری ضعیف، متناقض یا از نظر هندسی ناقص باشند، سیستم می‌تواند حالت‌هایی مثل `blank`، `ambiguous`، `multi_mark` یا `not_detected` را گزارش کند؛ به‌جای اینکه پاسخ را حدس بزند یا شماره سؤال‌ها را به‌صورت خاموش جابه‌جا کند.

هدف پروژه کاهش کار تکراری تصحیح و بررسی امکان استفاده از OMR نرم‌افزاری بدون وابستگی به اسکنرهای اختصاصی است.

---

## ۲. بیان مسئله

تصحیح دستی آزمون‌های چندگزینه‌ای با افزایش تعداد سؤال‌ها و دانش‌آموزان به کاری تکراری و زمان‌بر تبدیل می‌شود. برخی سامانه‌های سنتی OMR نیز به اسکنر اختصاصی یا فرم‌های بسیار کنترل‌شده وابسته هستند.

SmartSheet یک مسیر نرم‌افزاری را بررسی می‌کند: استفاده از عکس معمولی برگه و بینایی ماشین برای بازسازی شبکه پاسخ‌ها و تولید نتیجه‌ای قابل بررسی.

### مزایای موردنظر

- کاهش کار تکراری تصحیح
- ایجاد بررسی یکنواخت توسط ماشین
- پشتیبانی از برگه‌های عکس‌برداری‌شده و اسکن‌شده
- نمایش صریح حالت‌های خالی، مبهم، چندعلامتی و تشخیص‌داده‌نشده
- تولید خروجی ساختاریافته برای استفاده‌های بعدی
- کاهش وابستگی به سخت‌افزار اختصاصی OMR در سناریوهای مناسب

---

## ۳. دقیقاً چه چیزی در پروژه پیاده‌سازی شده است؟

هسته فعلی پروژه بر الگوریتم‌های قطعی بینایی ماشین و امتیازدهی عددی استوار است. اجزای پیاده‌سازی‌شده شامل موارد زیر هستند:

- پردازش تصویر با OpenCV
- تشخیص محدوده برگه و اصلاح پرسپکتیو چهار نقطه‌ای در صورت یافتن مرز مناسب
- اصلاح چرخش جزئی با Hough Line در صورت خراب بودن مرز خارجی برگه
- بهبود کنتراست با CLAHE
- Adaptive Thresholding
- تشخیص کاندیدا با Hough Circle
- تشخیص کاندیدا با Contour Analysis
- حذف کاندیداهای تکراری و فیلتر هندسی
- بازسازی شبکه فیزیکی/هندسی سؤال‌ها
- امتیازدهی چندویژگی به حباب‌ها
- تصمیم‌گیری محافظه‌کارانه درباره وضعیت پاسخ
- مقایسه با کلید پاسخ
- تولید گزارش JSON و CSV و تصویر تحلیل‌شده

همچنین سیستم کیفیت تصویر را از نظر رزولوشن، کنتراست، شارپنس و روشنایی بررسی می‌کند و در صورت احتمال کاهش قابلیت اطمینان هشدار می‌دهد.

---

## ۴. نوآوری اصلی

ایده اصلی فنی SmartSheet ترکیب **تشخیص کاندیدا + بازسازی هندسی شبکه پاسخ‌ها** است.

تشخیص دایره ممکن است یک حباب ضعیف یا ناقص را پیدا نکند. اگر شماره سؤال‌ها فقط از حباب‌های پیدا‌شده استخراج شوند، گم‌شدن یک حباب می‌تواند باعث جابه‌جایی تفسیر سؤال‌های بعدی شود.

SmartSheet به‌جای این کار از حباب‌های تشخیص‌داده‌شده به‌عنوان شواهد هندسی استفاده می‌کند، شبکه فیزیکی ردیف‌ها و ستون‌ها را تخمین می‌زند و موقعیت‌های مورد انتظار را نیز ارزیابی می‌کند. اگر شواهد یک ردیف کامل کافی نباشد، ردیف `not_detected` گزارش می‌شود و شماره سؤال‌های بعدی به‌صورت خاموش جابه‌جا نمی‌شوند.

### اهمیت این طراحی

این رویکرد شکست سیستم را قابل مشاهده‌تر و ایمن‌تر می‌کند: به‌جای تولید یک پاسخ ظاهراً مطمئن از روی داده ناقص، مورد مشکوک برای بررسی مشخص می‌شود.

---

## ۵. معماری سامانه

```text
تصویر ورودی
    │
    ▼
بارگذاری و بررسی کیفیت
    ├── رزولوشن
    ├── کنتراست
    ├── شارپنس
    └── روشنایی
    │
    ▼
اصلاح هندسی برگه
    ├── تشخیص Contour برگه
    ├── Perspective Transform چهار نقطه‌ای
    └── مسیر جایگزین اصلاح چرخش
    │
    ▼
پیش‌پردازش
    ├── Grayscale
    ├── CLAHE
    ├── Gaussian Smoothing
    └── Adaptive Thresholding
    │
    ▼
تشخیص کاندیداها
    ├── Hough Circles
    ├── Contours
    ├── تخمین اندازه حباب
    └── حذف تکراری‌ها / فیلتر
    │
    ▼
بازسازی شبکه
    ├── گروه‌بندی ستون‌های فیزیکی
    ├── خوشه‌بندی ردیف‌ها
    ├── تخمین Pitch
    ├── برازش شبکه ردیفی
    └── نگاشت شماره سؤال
    │
    ▼
امتیازدهی حباب
    ├── Dark Ratio
    ├── Very-Dark Ratio
    ├── Contrast
    ├── Fill Ratio
    └── Center Darkness
    │
    ▼
لایه تصمیم
    ├── Blank
    ├── Answered
    ├── Ambiguous
    ├── Multi-mark
    └── Not detected
    │
    ▼
تصحیح و خروجی
    ├── مقایسه با کلید
    ├── گزارش
    ├── JSON
    ├── CSV
    └── تصویر تحلیل‌شده
```

---

## ۶. پیاده‌سازی فنی

### ۶.۱ بررسی کیفیت تصویر

قبل از تصحیح، سیستم کیفیت تصویر را با استفاده از رزولوشن، آمار شدت خاکستری، کنتراست، شارپنس مبتنی بر لاپلاسیَن و روشنایی تخمین می‌زند. این مقادیر عمدتاً به‌عنوان هشدار استفاده می‌شوند و لزوماً باعث رد خودکار تصویر نمی‌شوند.

### ۶.۲ اصلاح محدوده و پرسپکتیو برگه

سیستم ابتدا به دنبال یک Contour چهارضلعی بزرگ می‌گردد که بتواند محدوده برگه را نشان دهد. در صورت موفقیت، نقاط مرتب و تصویر با Perspective Transform چهار نقطه‌ای به نمای مستطیلی نرمال تبدیل می‌شود.

اگر مرز خارجی برگه شکسته باشد، مسیر جایگزین Hough Line می‌تواند چرخش جزئی تصویر را تخمین زده و آن را صاف کند. اگر این روش‌ها قابل اعتماد نباشند، تصویر اصلی حفظ می‌شود و هشدار صادر می‌گردد.

### ۶.۳ پیش‌پردازش

پیاده‌سازی فعلی شامل این مراحل است:

1. تبدیل به Grayscale
2. بهبود کنتراست با CLAHE
3. Gaussian Smoothing
4. Adaptive Gaussian Thresholding
5. تخمین شدت پس‌زمینه

### ۶.۴ تشخیص کاندیدا

دو منبع مکمل برای تشخیص حباب استفاده می‌شود:

1. **Hough Circle Transform** برای حباب‌های دایره‌ای
2. **Contour Analysis** برای اشکالی که ممکن است با Hough به‌خوبی تشخیص داده نشوند

کاندیداها فیلتر و از نظر مکانی Deduplicate می‌شوند. اندازه واقعی حباب نیز پیش از مرحله اصلی تشخیص تخمین زده می‌شود تا اشکال نامرتبط صفحه کمتر به‌عنوان حباب پذیرفته شوند.

### ۶.۵ بازسازی شبکه سؤال‌ها

شبکه بر اساس هندسه مکانی کاندیداها ساخته می‌شود و هر تشخیص به‌صورت مستقل به‌عنوان یک سؤال در نظر گرفته نمی‌شود.

مراحل اصلی:

1. گروه‌بندی کاندیداها در ستون‌های فیزیکی
2. تخمین فاصله افقی و عمودی
3. خوشه‌بندی ردیف‌ها
4. برازش شبکه ردیفی با استفاده از فاصله منظم و شواهد موجود
5. ایجاد موقعیت مورد انتظار هر حباب
6. امتیازدهی موقعیت‌های مورد انتظار حتی در صورت گم‌شدن یک دایره Hough
7. گزارش `not_detected` زمانی که شواهد ساختاری کافی نیست

### ۶.۶ امتیازدهی حباب

هر گزینه با چند ویژگی تصویری ارزیابی می‌شود:

| ویژگی | نقش |
|---|---|
| Dark Ratio | نسبت پیکسل‌های تیره داخل ناحیه حباب |
| Very-Dark Ratio | شواهد علامت قوی مداد یا خودکار |
| Contrast | مقایسه تیرگی حباب با شدت محلی/پس‌زمینه |
| Fill Ratio | تخمین میزان پرشدگی حباب |
| Center Darkness | استفاده از تیرگی مرکز برای تقویت شواهد |

فرمول امتیاز فعلی:

```python
score = (
    0.34 * dark +
    0.22 * very_dark +
    0.22 * contrast +
    0.14 * fill_ratio +
    0.08 * center_dark
)
```

### ۶.۷ آستانه‌های تصمیم

پارامترهای فعلی شامل موارد زیر هستند:

```python
MARK_FLOOR = 0.145
STRONG_MARK_FLOOR = 0.205
MULTI_MARGIN = 0.045
MAX_MARKED = 2
```

این مقادیر **استاندارد جهانی OMR نیستند**؛ بلکه پارامترهای پیاده‌سازی هستند و برای طراحی‌های مختلف برگه، دوربین، نور، کیفیت کاغذ و ابزار نوشتاری ممکن است نیاز به تنظیم داشته باشند.

### ۶.۸ وضعیت‌های تصمیم

| وضعیت | معنی |
|---|---|
| `answered` | یک پاسخ با اطمینان کافی برای پذیرش انتخاب شده است. |
| `blank` | هیچ گزینه‌ای به حداقل آستانه علامت نرسیده است. |
| `ambiguous` | شواهد برای تفکیک مطمئن یک گزینه از گزینه دیگر کافی نیست. |
| `multi_mark` | دو یا چند گزینه به‌صورت قوی علامت‌خورده یا متعارض تشخیص داده شده‌اند. |
| `not_detected` | ردیف سؤال با شواهد هندسی کافی بازسازی نشده است. |

پس از تشخیص، پاسخ انتخاب‌شده با کلید مقایسه می‌شود و نتیجه‌هایی مانند Correct یا Wrong تولید می‌شود.

### ۶.۹ Confidence

Confidence فعلی یک شاخص پشتیبان تصمیم است و از قدرت علامت و میزان فاصله آن از گزینه دوم محاسبه می‌شود:

```python
confidence = 100 * (0.60 * absolute + 0.40 * separation)
```

- `absolute`: قدرت نرمال‌شده بهترین گزینه بالاتر از آستانه
- `separation`: میزان برتری بهترین گزینه نسبت به گزینه دوم

**نکته مهم:** Confidence فعلی یک احتمال آماری کالیبره‌شده نیست. برای کالیبراسیون آماری به دیتاست برچسب‌خورده و مستقل نیاز است.

---

## ۷. ارزیابی و شواهد پروژه

در حال حاضر **دو سطح متفاوت از شواهد تست** در پروژه وجود دارد و نباید آن‌ها را در مسابقه با هم قاطی کرد.

### ۷.۱ بنچمارک کنترل‌شده قبلی

در مستندات قبلی پروژه، نتیجه ۱۰ برگه آزمون کنترل‌شده با میانگین ثبت‌شده **95.2٪** آمده است. این عدد باید دقیقاً به‌عنوان **Controlled Benchmark / بنچمارک کنترل‌شده** معرفی شود، نه دقت تضمینی در دنیای واقعی.

| آزمون | نتیجه ثبت‌شده |
|---|---:|
| Exam 1 | 96% |
| Exam 2 | 100% |
| Exam 3 | 96% |
| Exam 4 | 80% |
| Exam 5 | 100% |
| Exam 6 | 98% |
| Exam 7 | 94% |
| Exam 8 | 100% |
| Exam 9 | 92% |
| Exam 10 | 96% |
| **میانگین ثبت‌شده** | **95.2%** |

> این رکورد به‌تنهایی اطلاعات کافی برای یک مطالعه اعتبارسنجی مستقل و واقعی در اختیار نمی‌گذارد. به‌خصوص تعریف دقیق «Accuracy» و روش تعیین Ground Truth باید پیش از استفاده از 95.2٪ به‌عنوان ادعای علمی اصلی مستند شود.

### ۷.۲ شواهد جدید Stress Test

یک تست استرس شش‌سناریویی نیز در پوشه شواهد پروژه وجود دارد. این مجموعه شامل **۲۰۵ سؤال در شش سناریوی کنترل‌شده/تولیدشده** است و **۱۴۷ پاسخ درست** ثبت کرده که نتیجه وزنی آن تقریباً **71.7٪** است.

مهم‌تر از عدد کلی، الگوی شکست‌هاست:

| سناریو | سؤال | درست | نتیجه ثبت‌شده |
|---|---:|---:|---:|
| تمیز، ۴۰ سؤال، ۲ ستون | 40 | 40 | 100.0% |
| یک ستون، ۴۰ سؤال، چرخش/نویز | 40 | 5 | 12.5% |
| Blur + Noise | 30 | 30 | 100.0% |
| Shadow + Decoys | 30 | 30 | 100.0% |
| Perspective + Rotation | 30 | 7 | 23.3% |
| Light Marks | 35 | 35 | 100.0% |
| **مجموع وزنی** | **205** | **147** | **71.7%** |

این تست‌ها یک ضعف فعلی مهم را آشکار می‌کنند: در برخی حالت‌های یک‌ستونه و پرسپکتیو شدید، بازسازی شبکه سؤال‌ها و تشخیص پاسخ افت قابل توجهی دارد. این موضوع باید به‌عنوان **محدودیت فعلی و هدف توسعه** مطرح شود، نه پنهان شود.

### ۷.۳ چه ادعایی برای مسابقه مناسب است؟

عبارت پیشنهادی:

> **در تست‌های کنترل‌شده، SmartSheet در چندین سناریوی تمیز، نویزی، دارای سایه و علامت‌های کم‌رنگ عملکرد خوبی نشان داد؛ در عین حال تست‌های استرس ضعف‌هایی را در شرایط پرسپکتیو شدید و برخی چیدمان‌های تک‌ستونه آشکار کردند. این نتایج مستقیماً مسیر بهبود Robustness سیستم را مشخص می‌کنند.**

از ادعاهایی مثل «100٪ دقیق»، «روی هر گوشی کار می‌کند» یا «برای همه برگه‌ها آماده استفاده نهایی است» تا زمانی که دیتاست واقعی و مستقل وجود نداشته باشد استفاده نشود.

---

## ۸. خروجی‌ها

پیاده‌سازی می‌تواند خروجی‌های زیر را تولید کند:

- گزارش قابل خواندن
- تصویر تحلیل‌شده و حاشیه‌نویسی‌شده
- گزارش JSON
- نتایج CSV

ساختار خروجی‌ها به‌گونه‌ای است که تصمیم هر سؤال قابل بررسی و استفاده مجدد در سیستم‌های بعدی باشد.

---

## ۹. اثر آموزشی — SDG 4

SmartSheet با **SDG 4: Quality Education** هم‌راستا است؛ زیرا یک گلوگاه اداری و تکراری در ارزیابی را هدف قرار می‌دهد و قصد جایگزینی معلم را ندارد.

### اثر بالقوه

- کاهش زمان کار تکراری تصحیح
- دسترسی سریع‌تر به نتایج
- بررسی اولیه یکنواخت‌تر
- ایجاد موارد قابل بازبینی برای پاسخ‌های نامطمئن
- نیاز کمتر به سخت‌افزار اختصاصی OMR در سناریوهای مناسب

پروژه باید به‌عنوان **فناوری کمک‌کننده به معلم** معرفی شود. بررسی انسانی برای موارد مبهم، چندعلامتی یا دارای مشکل ساختاری همچنان مهم است.

---

## ۱۰. رویکرد مسئولانه در فناوری

SmartSheet باید با چهار اصل معرفی شود:

1. **شفافیت:** توضیح اینکه تصمیم‌ها چگونه ساخته می‌شوند.
2. **نمایش عدم قطعیت:** تبدیل نکردن شواهد ضعیف به پاسخ ظاهراً قطعی.
3. **نظارت انسانی:** امکان بررسی موارد مشکل‌دار توسط مدرس.
4. **ادعاهای مبتنی بر شواهد:** جداکردن تست کنترل‌شده از اعتبارسنجی واقعی.

این موارد برای یک مسابقه فناوری آموزشی مزیت محسوب می‌شوند، چون طراحی فنی را به استقرار مسئولانه متصل می‌کنند.

---

## ۱۱. محدودیت‌ها

محدودیت‌هایی که در تست‌ها نیز دیده شده‌اند:

- پرسپکتیو شدید می‌تواند قابلیت بازسازی شبکه را به‌طور جدی کاهش دهد.
- برخی چیدمان‌های تک‌ستونه باعث از دست رفتن تعداد قابل توجهی از ردیف‌ها یا خطای تشخیص می‌شوند.
- علامت‌های بسیار کم‌رنگ یا غیرمعمول ممکن است به کالیبراسیون نیاز داشته باشند.
- هندسه‌های متفاوت برگه ممکن است پارامترهای متفاوت بخواهند.
- هسته فعلی Deep Learning آموزش‌دیده نیست و عمدتاً Computer Vision قطعی است.
- شواهد فعلی هنوز یک دیتاست بزرگ و مستقل از عکس‌های واقعی نیست.
- Confidence فعلی احتمال آماری کالیبره‌شده نیست.

---

## ۱۲. نقشه راه توسعه

### فاز ۱ — Robustness

- بهبود پشتیبانی از حالت تک‌ستونه
- تقویت اصلاح پرسپکتیو در عکس‌های دشوار
- افزایش اعتبارسنجی هندسی قبل از پذیرش شبکه
- ساخت Regression Test برای هر شکست جدید

### فاز ۲ — کالیبراسیون مبتنی بر داده

- جمع‌آوری دیتاست متنوع از عکس‌های واقعی
- ایجاد Ground Truth در سطح سؤال
- تنظیم آستانه‌ها با Train/Validation Split
- کالیبره‌کردن Confidence
- گزارش Precision، Recall، Confusion Matrix و Failure Rate

### فاز ۳ — بینایی مبتنی بر AI

- بررسی CNN/Deep Learning برای تشخیص حباب یا طبقه‌بندی علامت
- مقایسه مدل یادگیری با Baseline قطعی فعلی
- حفظ لایه Conservative Review برای پیش‌بینی‌های نامطمئن

### فاز ۴ — پلتفرم آموزشی

- داشبورد معلم و کلاس
- تحلیل عملکرد دانش‌آموز
- اتصال به LMS
- گردش‌کار ثبت تصویر با موبایل
- رابط چندزبانه

---

## ۱۳. نصب و استفاده

نسخه دقیق وابستگی‌ها و آدرس Repository باید از **فایل‌های واقعی پروژه نهایی** برداشته شود. در نسخه مسابقه‌ای نباید URL ساختگی GitHub باقی بماند.

روند معمول اجرای محلی:

```bash
pip install -r requirements.txt
python app.py
```

سپس آدرس محلی Gradio که برنامه نمایش می‌دهد در مرورگر باز می‌شود.

### روند استفاده

1. عکس برگه را آپلود کنید.
2. کلید پاسخ را وارد کنید.
3. تعداد گزینه‌ها و ستون‌های فیزیکی را مطابق رابط برنامه تنظیم کنید.
4. تحلیل را اجرا کنید.
5. گزارش و تصویر تحلیل‌شده را بررسی کنید.
6. خروجی JSON و CSV را بررسی یا استخراج کنید.

---

## ۱۴. ساختار پروژه

در نسخه نهایی مسابقه باید **فقط فایل‌هایی که واقعاً داخل ZIP قرار گرفته‌اند** مستند شوند. ساختار معمول نسخه فعلی می‌تواند به شکل زیر باشد:

```text
SmartSheet/
├── app.py
├── exam_grader.py
├── requirements.txt
├── README.md
├── README_fa.md
├── DOCUMENTATION.md
├── LICENSE                 # فقط اگر واقعاً وجود دارد
└── sample_images/          # فقط اگر واقعاً وجود دارد
```

نباید وجود فایل، پوشه، Docker، Repository یا Dataset را ادعا کرد مگر اینکه واقعاً در بسته نهایی وجود داشته باشد.

---

## ۱۵. جایگاه پروژه برای مسابقه

### معرفی یک‌جمله‌ای

> **SmartSheet با استفاده از بینایی ماشین، تصاویر معمولی برگه‌های چندگزینه‌ای را به نتایج ساختاریافته و قابل بررسی تبدیل می‌کند؛ در نتیجه کار تکراری کاهش می‌یابد و موارد نامطمئن برای بررسی مدرس قابل مشاهده باقی می‌مانند.**

### معرفی کوتاه

> **SmartSheet یک سامانه نرم‌افزارمحور OMR برای آموزش است. این سیستم پردازش تصویر، بازسازی هندسی شبکه پاسخ، امتیازدهی چندویژگی علامت‌ها و منطق تصمیم‌گیری محافظه‌کارانه را ترکیب می‌کند تا تصحیح اولیه از روی عکس یا اسکن برگه انجام شود. طراحی سیستم عدم قطعیت را پنهان نمی‌کند و به‌جای حدس خاموش، موارد مشکل‌دار را برای بررسی انسان نمایش می‌دهد.**

### داستان اصلی پروژه

بهترین داستان پروژه این نیست که بگوییم «تصحیح کاملاً بدون خطا» است. داستان قوی‌تر و قابل دفاع‌تر این است:

**اتوماسیون قابل دسترس ارزیابی + بینایی ماشین شفاف + بررسی انسانی + پیشرفت مهندسی قابل اندازه‌گیری.**

---

## ۱۶. ساختار ویدئوی دمو — ۲:۳۰

| بخش | زمان |
|---|---:|
| مسئله / Hook | 0:00–0:20 |
| روش سنتی | 0:20–0:40 |
| معرفی SmartSheet | 0:40–1:05 |
| دمو واقعی برنامه | 1:05–1:40 |
| Pipeline بینایی ماشین | 1:40–2:05 |
| نتایج و خروجی‌ها | 2:05–2:20 |
| اثر SDG 4 | 2:20–2:25 |
| پایان | 2:25–2:30 |

در ویدئو باید اولویت با **تصاویر واقعی برنامه و شواهد واقعی تست** باشد. انیمیشن سینمایی برای توضیح مسئله، انتقال بین بخش‌ها و نمایش مفهومی Pipeline مناسب است، اما نباید قابلیت‌هایی را نشان دهد که نرم‌افزار فعلی واقعاً ندارد.

---

## ۱۷. چک‌لیست نهایی ارسال

### فنی

- [ ] کد نهایی در یک محیط تمیز اجرا شده است.
- [ ] فایل Dependency با کد واقعی یکسان است.
- [ ] هیچ GitHub URL ساختگی باقی نمانده است.
- [ ] اگر نام محصول نهایی SmartSheet است، نام قدیمی ExamVision در بخش‌های غیرضروری باقی نمانده است.
- [ ] نمونه ورودی‌ها در صورت اشاره در مستندات قرار گرفته‌اند.
- [ ] مثال‌های خروجی با فیلدهای واقعی برنامه مطابقت دارند.

### شواهد

- [ ] نتیجه 95.2٪ با عنوان Controlled Benchmark معرفی شده است.
- [ ] تست استرس جدید در مستندات پنهان نشده است.
- [ ] ادعاهای Accuracy، Robustness، AI و Readiness با شواهد سازگار هستند.
- [ ] محدودیت اعتبارسنجی واقعی بیان شده است.

### مسابقه

- [ ] مستندات انگلیسی کامل است.
- [ ] نسخه فارسی برای مرجع داخلی نگه داشته شده است.
- [ ] ویدئو در زمان تعیین‌شده قرار دارد.
- [ ] ZIP نهایی پس از بسته‌بندی تست شده است.
- [ ] نام پروژه، نسخه و ادعاهای فرم مسابقه با ZIP یکسان هستند.

---

## ۱۸. جمع‌بندی نهایی

SmartSheet نشان می‌دهد که چگونه بینایی ماشین می‌تواند بخشی از یک فرایند تکراری آموزشی را با استفاده از تصاویر معمولی و بدون نیاز به سخت‌افزار اختصاصی OMR خودکار کند. مهم‌ترین ارزش فنی پروژه ترکیب تشخیص کاندیداها، بازسازی هندسی شبکه، امتیازدهی چندویژگی حباب‌ها و مدیریت محافظه‌کارانه عدم قطعیت است.

نقطه قوت پروژه زمانی بیشتر می‌شود که محدودیت‌های آن نیز بخشی از داستان مهندسی باشند: تست‌های کنترل‌شده در چند شرایط عملکرد خوبی نشان داده‌اند و تست‌های استرس نیز مشخص کرده‌اند که در پرسپکتیو شدید و بعضی حالت‌های تک‌ستونه چه بخش‌هایی باید بهبود پیدا کنند.

**SmartSheet — ارزیابی هوشمندتر، اتوماسیون شفاف‌تر، پشتیبانی بهتر از مدرس.**
